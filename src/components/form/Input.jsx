import React from 'react';
import styles from '../../style/Input.module.css'; 

const Input = () => {
  return <input type="text" placeholder="Input" className={styles.input} />;
};

export default Input;
